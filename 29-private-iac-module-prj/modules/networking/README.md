## Terraform private module

- This private module is reusable component to create a VPC with a given CIDR block

- Allow the user to provide the configuration for multiple subnets

- It must be able to provide CIDR block

- It must be to mark a subnet as public and private

- It must provide AWS Availability Zones